apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "sglang-inference.fullname" . }}
  labels:
    {{- include "sglang-inference.labels" . | nindent 4 }}
  {{- if and (eq .Values.scheduler.type "volcano") (hasKey .Values.scheduler "volcano") (gt (int .Values.scheduler.volcano.groupMinMember) 0) }}
  annotations:
    scheduling.volcano.sh/group-min-member: {{ .Values.scheduler.volcano.groupMinMember | quote }}
  {{- end }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "sglang-inference.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- if or .Values.podAnnotations .Values.scheduler.annotations }}
      annotations:
        {{- with .Values.scheduler.annotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.podAnnotations }}
        {{- toYaml .Values.podAnnotations | nindent 8 }}
        {{- end }}
      {{- end }}
      labels:
        {{- include "sglang-inference.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- if .Values.runtimeClassName }}
      runtimeClassName: {{ .Values.runtimeClassName }}
      {{- end }}
      {{- if .Values.scheduler.type }}
      {{- if eq .Values.scheduler.type "hami" }}
      schedulerName: hami-scheduler
      {{- else if eq .Values.scheduler.type "volcano" }}
      schedulerName: volcano
      {{- end }}
      {{- end }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName }}
      {{- end }}
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "sglang-inference.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      {{- if .Values.initContainers }}
      initContainers:
        {{- range $i, $c := .Values.initContainers }}
        - name: {{ $c.name }}
          image: {{ $c.image }}
          command: {{ $c.command }}
          args: {{ $c.args }}
          volumeMounts:
            {{- toYaml $c.volumeMounts | nindent 12 }}
          resources:
            {{- toYaml $c.resources | nindent 12 }}
        {{- end }}
      {{- end }}
      containers:
      - name: sglang
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        {{- if .Values.model.downloadOnStartup }}
        command:
          - /bin/bash
          - -c
          - |
            {{- if .Values.distributed.enabled }}
            # Multi-node setup
            apt-get update && apt-get install -y ssh-client
            {{- end }}
            python3 -m sglang.launch_server {{ include "sglang-inference.engineArgs" . | indent 12 }}
        {{- else }}
        command:
          - /bin/bash
          - -c
          - |
            python3 -m sglang.launch_server {{ include "sglang-inference.engineArgs" . | indent 12 }}
        {{- end }}
        ports:
        - name: http
          containerPort: {{ .Values.engine.port }}
          protocol: TCP
        env:
        - name: HF_HOME
          value: "/model"
        - name: MODEL_NAME
          value: {{ .Values.model.name | quote }}
        {{- with .Values.extraEnv }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- if .Values.distributed.enabled }}
        - name: VLLM_RANK
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['vllm.ai/rank']
        - name: VLLM_TENSOR_PARALLEL_SIZE
          value: {{ .Values.engine.tensorParallelSize | quote }}
        - name: VLLM_PIPELINE_PARALLEL_SIZE
          value: {{ .Values.engine.pipelineParallelSize | quote }}
        {{- end }}
        livenessProbe:
          {{- toYaml .Values.livenessProbe | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.readinessProbe | nindent 10 }}
        {{- if .Values.startupProbe }}
        startupProbe:
          {{- toYaml .Values.startupProbe | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        {{- if or .Values.persistence.enabled .Values.shm.enabled .Values.volumeMounts }}
        volumeMounts:
        {{- if .Values.persistence.enabled }}
        - name: model-cache
          mountPath: {{ .Values.persistence.mountPath }}
        {{- end }}
        {{- if .Values.shm.enabled }}
        - name: dshm
          mountPath: /dev/shm
        {{- end }}
        {{- if .Values.volumeMounts }}
        {{- toYaml .Values.volumeMounts | nindent 8 }}
        {{- end }}
        {{- end }}
      {{- if .Values.sidecars }}
      {{- range .Values.sidecars }}
      - name: {{ .name }}
        image: {{ .image }}
        {{- if .command }}
        command: {{ .command }}
        {{- end }}
        {{- if .args }}
        args: {{ .args }}
        {{- end }}
        {{- if .env }}
        env:
          {{- toYaml .env | nindent 8 }}
        {{- end }}
        {{- if .volumeMounts }}
        volumeMounts:
          {{- toYaml .volumeMounts | nindent 8 }}
        {{- end }}
        {{- if .resources }}
        resources:
          {{- toYaml .resources | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- end }}
      {{- if or .Values.persistence.enabled .Values.shm.enabled .Values.volumes }}
      volumes:
      {{- if .Values.persistence.enabled }}
      - name: model-cache
        persistentVolumeClaim:
          claimName: {{ include "sglang-inference.fullname" . }}-model-cache
      {{- end }}
      {{- if .Values.shm.enabled }}
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: {{ .Values.shm.sizeLimit | default "1Gi" }}
      {{- end }}
      {{- if .Values.volumes }}
      {{- toYaml .Values.volumes | nindent 6 }}
      {{- end }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- $tolerations := concat (include "sglang-inference.gpuTolerations" . | fromYamlArray | default list) .Values.tolerations }}
      {{- if $tolerations }}
      tolerations:
        {{- toYaml $tolerations | nindent 8 }}
      {{- end }}
---
{{- if .Values.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "sglang-inference.fullname" . }}
  labels:
    {{- include "sglang-inference.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "sglang-inference.fullname" . }}
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.autoscaling.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
