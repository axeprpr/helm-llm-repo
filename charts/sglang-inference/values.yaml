# Default values for sglang-inference chart

replicaCount: 1

image:
  repository: lmsysorg/sglang
  pullPolicy: IfNotPresent
  tag: "latest-runtime"
imagePullSecrets: []

# Model configuration
model:
  # HuggingFace model ID or local path
  name: "Qwen/Qwen2.5-7B-Instruct"
  # Model format: auto, fp8, fp16, bf16, int8, awq, gptq
  format: auto
  # Download on startup (requires internet)
  downloadOnStartup: true
  # HF token for gated models
  hfToken: ""
  # Reasoning parser for reasoning models (auto, deepseek_r1, qwen3)
  reasoningParser: ""

# Engine configuration
engine:
  # Port for OpenAI-compatible API
  port: 8000
  # Extra engine arguments
  extraArgs: ""
  # Benchmark profile presets (GPUStack profiles_config.yaml)
  # Options: throughput (1024+128), latency (128+128), long-context (32000)
  benchmarkProfile: ""
  # Max model length
  maxModelLen: 8192
  # Tensor parallelism (for multi-GPU)
  tensorParallelSize: 1
  # Pipeline parallelism (for multi-node)
  pipelineParallelSize: 1
  # GPU memory fraction per GPU (0.0-1.0)
  gpuMemoryUtilization: 0.90
  # Enable prefix caching
  enablePrefixCaching: false
  # Enable chunked prefill
  enableChunkedPrefill: false

# Multi-node / distributed inference
distributed:
  enabled: false
  # Init container image for NCCL setup
  initImage: "nccl:latest"
  # Node list for multi-node (comma-separated hostnames)
  nodeList: []
  # Master address (usually first node)
  masterAddr: ""
  # Master port
  masterPort: 29500

# Scheduler type
scheduler:
  # native, hami (Xiaohongshu), volcano (Huawei)
  type: native
  # Scheduler-specific annotations
  annotations: {}
  volcano:
    queueName: ""
    groupMinMember: 0
    createPodGroup: false
    minResources: {}
  hami:
    nodeSchedulerPolicy: ""
    gpuSchedulerPolicy: ""
    gpuSharePercent: ""

# GPU hardware type
gpuType: nvidia
# Supported: nvidia, ascend, amd, intel, metal

# Resource limits
resources:
  limits:
    nvidia.com/gpu: "1"
    memory: "16Gi"
    cpu: "4"
  requests:
    cpu: "2"
    memory: "8Gi"

# Node selector
nodeSelector: {}
tolerations: []
affinity: {}

# Prometheus Operator integration
metrics:
  enabled: true
  path: /metrics
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    labels: {}
    annotations: {}

# Service configuration
service:
  type: ClusterIP
  port: 8000
  annotations: {}

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: sglang.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# Health checks
livenessProbe:
  httpGet:
    path: /model_info
    port: 8000
  initialDelaySeconds: 0
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /model_info
    port: 8000
  initialDelaySeconds: 10
  periodSeconds: 5
startupProbe:
  httpGet:
    path: /model_info
    port: 8000
  initialDelaySeconds: 10
  periodSeconds: 10
  failureThreshold: 90

# Autoscaling
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

# /dev/shm for NCCL shared memory
shm:
  enabled: false
  sizeLimit: 1Gi

# Persistence (for model cache)
persistence:
  enabled: false
  storageClass: ""
  size: 50Gi
  mountPath: /root/.cache/huggingface

# Pod disruption budget
podDisruptionBudget:
  enabled: false
  minAvailable: ""
  maxUnavailable: 1

# Pod annotations
podAnnotations: {}
podLabels: {}
extraEnv: []

# Security context
# Runtime class
runtimeClassName: ""

# Priority class
priorityClassName: ""

# Termination grace period
terminationGracePeriodSeconds: 60

# Topology spread
topologySpreadConstraints: []

# Security context
securityContext:
  runAsUser: 0
  runAsGroup: 0
  fsGroup: 0

# Init containers (e.g., model downloader)
initContainers: []

# Sidecar containers
sidecars: []

# Service account
serviceAccount:
  create: false
  name: ""
  annotations: {}

# Volumes and volume mounts
volumes: []
volumeMounts: []
