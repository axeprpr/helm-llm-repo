# Default values for vllm-inference chart

replicaCount: 1

image:
  repository: ghcr.io/vllm-project/vllm-openai
  pullPolicy: IfNotPresent
  tag: "0.18.0"

# Model configuration
model:
  # HuggingFace model ID or local path
  name: "Qwen/Qwen2.5-7B-Instruct"
  # chat, embedding, vision
  type: chat
  # Model format hint. The chart explicitly renders GPTQ/AWQ quantization flags;
  # other formats rely on the model's own config/defaults.
  format: auto
  # Download on startup (requires internet)
  downloadOnStartup: true
  # HF token for gated models
  hfToken: ""
  # Reasoning parser for reasoning-capable models
  reasoningParser: ""

# Engine configuration
engine:
  # Port for OpenAI-compatible API
  port: 8000
  # Extra engine arguments
  extraArgs: ""
  # Max model length
  maxModelLen: 8192
  # Embedding task
  embeddingTask: embed
  # Pooling function for embedding models
  poolerType: last
  # Tensor parallelism (for multi-GPU)
  tensorParallelSize: 1
  # Pipeline parallelism (for multi-node)
  pipelineParallelSize: 1
  # GPU memory fraction per GPU (0.0-1.0)
  gpuMemoryUtilization: 0.90
  # Enable prefix caching
  enablePrefixCaching: false
  # Enable chunked prefill
  enableChunkedPrefill: false

# Multi-node / distributed inference
distributed:
  enabled: false
  # Init container image for NCCL setup
  initImage: "nccl:latest"
  # Node list for multi-node (comma-separated hostnames)
  nodeList: []
  # Master address (usually first node)
  masterAddr: ""
  # Master port
  masterPort: 29500

# Scheduler type
scheduler:
  # native, hami (Xiaohongshu), volcano (Huawei)
  type: native
  # Explicit scheduler name override
  name: ""
  # Scheduler-specific annotations
  annotations: {}
  volcano:
    queueName: ""
    groupMinMember: 0
    createPodGroup: false
    minResources: {}
  hami:
    nodeSchedulerPolicy: ""
    gpuSchedulerPolicy: ""
    gpuSharePercent: ""

# GPU hardware type
gpuType: nvidia
# Supported: nvidia, ascend, amd, intel, metal

# Resource limits
resources:
  limits:
    nvidia.com/gpu: "1"
    memory: "16Gi"
    cpu: "4"
  requests:
    cpu: "2"
    memory: "8Gi"

# Node selector
nodeSelector: {}
tolerations: []
affinity: {}

# Service configuration
service:
  type: ClusterIP
  port: 8000
  annotations: {}

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: vllm.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

# Health checks
livenessProbe:
  httpGet:
    path: /health
    port: 8000
  initialDelaySeconds: 30
  periodSeconds: 10
readinessProbe:
  httpGet:
    path: /health
    port: 8000
  initialDelaySeconds: 10
  periodSeconds: 5
startupProbe: {}

# Autoscaling
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

# Persistence (for model cache)
persistence:
  enabled: false
  storageClass: ""
  size: 50Gi
  mountPath: /root/.cache/huggingface

# Pod annotations
podAnnotations: {}
podLabels: {}

# Extra environment
extraEnv: []
envFrom: []
command: []
args: []

# Runtime and disruption controls
runtimeClassName: ""
priorityClassName: ""
terminationGracePeriodSeconds: 120
topologySpreadConstraints: []

# Shared memory for NCCL / tensor parallelism
shm:
  enabled: true
  sizeLimit: 1Gi

# Security context
securityContext:
  runAsUser: 0
  runAsGroup: 0
  fsGroup: 0

# Init containers (e.g., model downloader)
initContainers: []

# Sidecar containers
sidecars: []

# Service account
serviceAccount:
  create: true
  name: ""
  annotations: {}

# Pod disruption budget
podDisruptionBudget:
  enabled: false
  minAvailable: ""
  maxUnavailable: 1

# GPUStack-style model catalog ConfigMap (recommended models + flags)
# Reference: GPUStack model-catalog.yaml (87 model sets)
modelCatalog:
  enabled: false
  # Selected preset: auto-fills recommended engine flags when model matches
  # Options:
  #   qwen2.5-7b-chat    - Qwen2.5-7B-Instruct
  #   qwen2.5-72b-chat   - Qwen2.5-72B-Instruct
  #   llama-70b-chat      - Llama-3.1-70B-Instruct
  #   deepseek-r1-7b     - DeepSeek-R1-Distill-Qwen-7B (with reasoningParser)
  #   bge-m3-embed        - BAAI/bge-m3 (embedding task)
  #   qwen2vl-7b-vision   - Qwen/Qwen2-VL-7B-Instruct
  # When set, chart will auto-configure engine flags from catalog
  selectedPreset: ""
  # Includes: chat-models.yaml, embedding-models.yaml, vision-models.yaml
  # Use: kubectl get configmap -l app.kubernetes.io/component=model-catalog

# Prometheus Operator integration
metrics:
  enabled: true
  path: /metrics
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    labels: {}
    annotations: {}

# Volumes and volume mounts
volumes: []
volumeMounts: []
